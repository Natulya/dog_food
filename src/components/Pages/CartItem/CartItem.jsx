/*
import { useDispatch } from "react-redux"

export const CartItem = ({name, pictures, price, id,}) => {

const dispatch = useDispatch()

const deleteProductHandler = () => {
    dispatch(deleteProduct(id))
}

    return (
        <li>
            <img src={pictures} alt={} />
            <div>
                <h5>{name}</h5>
                <p>{price}</p>
            </div>

        </li>
    )
}
*/
